from setuptools import setup, find_packages

setup(
    name="ascii_art_generator",
    version="1.0.0",
    description="Модуль для генерації ASCII-фігур",
    author="Вlxxd", 
    packages=find_packages(),
    install_requires=[],
)
